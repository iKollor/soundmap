import MapView from '@/components/MapView';

export const metadata = {
    title: 'Explorar | Mapa Sonoro',
    description: 'Explora y escucha los sonidos de en un mapa interactivo',
};

export default function ExplorePage() {
    return <MapView />;
}
